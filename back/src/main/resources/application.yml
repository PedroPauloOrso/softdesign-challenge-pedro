spring:
  datasource:
    url: jdbc:mysql://localhost:3306/softdesign_challenge
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    sql: debug
    org.hibernate.orm.jdbc.bind: trace
